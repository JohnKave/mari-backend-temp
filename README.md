User authentication done
authorization not included
auth service and registration service (UserManagementService) are separated
---------------------------------------------------------------------------
registration will not generate token
login to get token

can test the token/login at product endpoint
***SECRET_KEY still isn't moved yet << ! must be moved before deployment
